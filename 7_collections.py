people = ['Sam', 'Josie', 'Bob']
len(people)

stuff = ('hello', 'world', 'bye', 'now')

stuff = ('hello', 'world', 'goodbye', 'now')

"""
lists are mutable, tuples arent
lists are ordered
both are ordered
both are heterogeneous
"""

# strings are ordered and can be accessed by their index

# sets cant be duplicated, and are unordered

pi = 3.141592
str_pi = str(pi)

range(7) # 0 -> 6
range(1, 6) # 1 -> 5
range(3, 15, 4) # 3, 7, 11
range(3, 8, -1) # empty
range(8, 3, -1) # 8,7,6,5,4

list(range(3, 17, 4))

#yes, no, yes, yes

# no sets are unordered

origin = {
    'Alice': 'USA',
    'Francois': 'Canada',
    'Inti' : 'Peru',
    'Monika' : 'Germany',  
    'Sanyu'	: 'Uganda',
    'Yoshitaka' : 'Japan'}